import { Routes, RouterModule } from '@angular/router';
import { InterviewedCandidateComponent } from '../InterviewedCandidate.Component';

export const InterviewedCandidateRoutes: Routes = [
  { path: '', component: InterviewedCandidateComponent },
  
];
