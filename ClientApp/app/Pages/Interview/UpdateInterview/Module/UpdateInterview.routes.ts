import { Routes, RouterModule } from '@angular/router';
import { UpdateInterviewComponent } from '../UpdateInterview.Component';

export const UpdateInterviewRoutes: Routes = [
  { path: '', component: UpdateInterviewComponent },
  
];
