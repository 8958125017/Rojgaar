import { Routes, RouterModule } from '@angular/router';
import { showUserComponent } from '../showUser.Component';

export const showUserRoutes: Routes = [
  { path: '', component: showUserComponent },
  // { path: 'index', component: YuvaComponent }
];
