import { Routes, RouterModule } from '@angular/router';
import { ReviewApplicationComponent } from '../reviewApplication.Component';

export const ReviewApplicationRoutes: Routes = [
  { path: '', component: ReviewApplicationComponent },
  // { path: 'index', component: YuvaComponent }
];
